// Popup bootstrap using shared controller.js only
(async()=>{const root=document.getElementById('app-root');if(!root)return;if(!window.TabsyUIController){console.error('TabsyUIController script missing');return;}const controller=new window.TabsyUIController(root);window.tabsyPopup=controller;await controller.init();})();